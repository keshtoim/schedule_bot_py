services:
  bot:
    build: .
    image: schedule-bot
    container_name: schedule-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - bot-data:/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  bot-data:
